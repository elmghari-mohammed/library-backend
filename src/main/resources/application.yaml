spring:
  application:
    name: demo
  profiles:
    active: dev
  graphql:
    http:
      path: /biblio
    graphiql:
      enabled: true
  datasource:
    url: jdbc:h2:mem:bibliotheque
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
